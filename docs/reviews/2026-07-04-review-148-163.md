# Post-merge design + code review — #148/#163 multi-issue driver

Date: 2026-07-04 · Reviewer: Fable 5 (xhigh) · Subject: PR #185 (squash `d7ea584`, v2.64.0)
· Method: every code finding **executed against the merged module**, not read-inferred
· Visual: `docs/planning/2026-07-04-review-148-163.html`

## Verdict

The architecture holds. Pattern-not-skill was the right cut (the driver structurally
cannot weaken WF2 — it owns no WF2 step); the committed-contract vs disk-persisted-state
split is honest about the gitignored `claude_docs/`; the narrow-helper scope discipline
matches #134. Defects live at the edges: one design contradiction inherited from #134's
own ambiguity (F1), a dependency parser whose in-slot review fixes traded over-capture
for common-phrasing under-capture (F2/F3), and spec gaps a first real campaign would
trip on (F4–F7). Nothing invalidates the merge.

**Resolution: all 7 findings FIXED in the follow-up hardening PR** (this file rides it):
F1–F4 code+tests (opus implementer, TDD, red-first confirmed), F1-doc/F5/F6/F7 doc+schema
(sonnet implementer), each collected-and-verified by the orchestrator's own gate.

## Findings

| # | Sev | Finding | Evidence (executed) | Resolution |
|---|---|---|---|---|
| F1 | High (design) | Serial-active invariant (`≤1 in_progress/pr_open`) rejected the exact state the #163-AC3 `deps_satisfied_by="pr_open"` knob creates (headless stacked PRs). Both sides were test-locked. Root: design #134 L58 ("pr_open terminal") vs L97 ("advance on PR creation"). | `{#1 pr_open, #2 in_progress}` → `validate_driver_state` FAILED | Invariant relaxed to `in_progress ≤ 1`; `pr_open` accumulates. Doc L43 rewritten; test flipped. **Owner fork taken: recommended option** (Decisions log). |
| F2 | Medium | Parser silently dropped the most common phrasing — missing ordering edge = premature start. | `"Depends on issue #10."` → `[]`; `"Blocked by issues #10 and #20."` → `[]` | `_DEP_LIST_RE` widened with optional `(?:the )?(?:issues?\|prs?\|epics?)` noun before each `#N`; +5 regression tests. Sentence-boundary behavior preserved. |
| F3 | Med-Low | Modal negations were dead letters — real English puts "be" between modal and phrase. | `"cannot be blocked by #5"` → `[5]` | `_NEG_BEFORE_RE` gains optional `be\|get` bridge; +3 regression tests. |
| F4 | Med-Low | String-typed `depends_on` (`["148"]`) silently imposed no edge in topo AND advance (filtered as "external"). | `next_ready_issue` returned the dependent before its dependency | `_in_queue_deps` raises typed `DriverStateError` on non-int entries (both DAG entry points fail closed); doc §Resumption mandates validate-on-every-load. |
| F5 | Med-Low | "Ledger" referenced twice in the doc, defined nowhere (design #134's alignment-ledger lost its definition). | doc L124, L224 | New "### The ledger" subsection: the ledger IS the state file (per-issue deferred fields + top-level `notes[]`); `notes` added to `queue.schema.json`. |
| F6 | Low | "Parked" was two contradictory states — DEFER table said status→`deferred`+reason; code skips-while-`queued`. | table row vs `next_ready_issue` behavior | Table row + §Dependency-ordering reworded to the code's semantics (implicit skip; `deferred` only if dependency `abandoned`/campaign-end). |
| F7 | Low | Epic body fed to `parse_depends_on` returns all children as "dependencies" (same `- [ ] #N` syntax, two roles). | epic body → `[201, 202]` | Epic-anchor bullet added: never dependency-parse the epic body. |

Note-only (accepted): "or"-separated alternatives both captured; `next_ready_issue`
takes `deps_satisfied_by` as a parameter (caller passes policy value); AC2's literal
`claude_docs/.driver-state/` committed-path deviation was Decision-logged in-slot;
the "0 ordering violations" success metric remains unit-proxied until a real
dependency-ordered campaign runs (the paused M3 tail is that proving run).

## Verified clean

Kahn + cycle machinery (deterministic tie-break; duplicate deps don't corrupt
in-degrees; self-cycle raises `#5 -> #5`; `_find_cycle` terminates; duplicate numbers
rejected). All in-slot review fixes held on re-execution. Schema ↔ validator ↔
examples: no drift (jsonschema hard-import, both examples pass both validators).
Tests non-vacuous (exact-value asserts; the not-markdown-aware limitation is locked
as documented behavior).

## AC coverage

#148 AC1–5 met (AC2 path deviation justified + logged). #163 AC1–7 met, with AC1
weakened by F2 (now fixed), AC3 conflicted by F1 (now resolved), AC4 ambiguous per
F6 (now reworded). AC5's one-way mirror is procedural (fine for a pattern; the
headless-refuse is executable via `validate_campaign_start`).
