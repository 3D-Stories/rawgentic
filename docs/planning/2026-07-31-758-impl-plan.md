# #758 implementation plan (small-standard lane checklist)

Branch: `feature/758-goal-verbatim-carry`. Design: `2026-07-31-758-goal-verbatim-carry-design.md` rev 4.
TDD red-before-green per task; scoped suite during iteration = `tests/hooks/test_adhoc_pane_handoff.py` + `tests/test_pane_handoff_skill.py`.

### Task 1: Pure helpers — live_owner_goal + validate_goal_carry
- riskLevel: high (security surface — sentinel-only trust closes the forged-row spoof; deserialization of transcript rows)
- files: hooks/launcher_lib.py, tests/hooks/test_adhoc_pane_handoff.py
- verification: unit rows red→green — sentinel-only selection (forged sentinel-less unmet row ignored; forged sentinel-less met:true cannot spoof cleared), last-sentinel-row liveness, armed-form exact comparison, single trailing-newline normalization, approved_answer override, None-predecessor pass, numeric-offset-only mismatch reason
- commit: feat(handoff): sentinel-only live_owner_goal + validate_goal_carry helpers (#758)

### Task 2: perform_handoff strict goal binding
- riskLevel: high (non-trivial error flow on the destructive teardown path)
- files: hooks/launcher_lib.py, tests/hooks/test_adhoc_pane_handoff.py
- verification: red→green — strict_goal_binding=True + expected_predecessor_goal snapshot (str|None): A→B re-arm → clear REFUSED, pane open, predecessor_guard names refusal without goal content; cleared→B (expected None, goal appears) → clear refused, pane open; defaults False/None → existing callers byte-identical (existing suite green untouched)
- commit: feat(handoff): strict goal binding refuses the clear on a diverged guard (#758)

### Task 3: ad-hoc-handoff CLI wiring
- riskLevel: high (input validation at a trust boundary; fail-closed decision)
- files: hooks/launcher_lib.py, tests/hooks/test_adhoc_pane_handoff.py
- verification: red→green — teardown path: _SESSION_ID_RE + containment before path build; missing/unreadable transcript → LauncherError (fail closed); differing successor goal → refused exit 2, perform_handoff never called; identical → proceeds with strict binding armed; --goal-rewrite-approved '<answer>' (required value) → proceeds, answer in output JSON; --no-teardown → no validation
- commit: feat(handoff): ad-hoc teardown validates verbatim goal carry (#758)

### Task 4: pane-handoff SKILL.md goal-carry rewrite + prose guards
- riskLevel: standard
- files: skills/pane-handoff/SKILL.md, tests/test_pane_handoff_skill.py
- verification: red→green — canonical verbatim-carry sentence pinned; AskUserQuestion//ask-owner approval rule present; "in its own words" gone; >500-char-paste prohibition present
- commit: feat(skills): pane-handoff carries the owner goal verbatim (#758)

### Task 5: version ×4 + README changelog + docs
- riskLevel: standard
- files: .claude-plugin/plugin.json, plugins/rawgentic/.codex-plugin/plugin.json, tests/hooks/test_adversarial_review_registration.py, phase_executor/src/phase_executor/canary.py, README.md
- verification: 3.109.8 → 3.110.0 on all four surfaces; changelog entry with diagram decision (no workflow-spine change → no REV) + Suite old→new tail; version-pin tests green
- commit: feat(handoff,skills): owner-authored goals carried verbatim across pane-handoffs (#758)
