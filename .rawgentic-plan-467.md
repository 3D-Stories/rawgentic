# WF2 Step 5 — Implementation Plan: #467 W4 tmux supervisor (iteration-3 design)

Modules (extraction-clean, NO hooks import): phase_executor/src/phase_executor/{registry.py, pane_runner.py, supervisor.py}.
Tests tests/phase_executor/. TDD red-before-green. Baseline 3634 pass / 10 skip.
Design of record: docs/planning/2026-07-18-467-tmux-supervisor.md (iteration 3, CF-1..CF-20).
Speed levers applied: scoped `pytest tests/phase_executor/` during iteration; full suite only at baseline + Step-9/final.

### Task 1: registry.py — durable job registry (pure core + atomic I/O)
- riskLevel: high (recovery trust boundary + reaper decisions gate two-group process kills)
- files: phase_executor/src/phase_executor/registry.py, tests/phase_executor/test_registry.py
- JobRecord (both pgids: pane_pgid + provider_pgid CF-17; all 6 WorktreeHandle fields CF-4; permit_ref CF-19;
  resume_attempts CF-6; provider_exit_code; attempt_id), state enum.
- session_name via worktree.component_for (CF-11); command_digest; handle_from_record (reconstruct frozen
  WorktreeHandle CF-4); classify_recovery (adopt/quarantine/relaunch<cap/fail CF-6/CF-7); reap_plan (3 tiers,
  kill gated on BOTH-group confirmed-death BEFORE clean-probe CF-8/CF-17).
- JobRegistry: atomic jobs.json (0700), upsert/get/by_run/all; single-writer doc note.

### Task 2: pane_runner.py — in-pane emitter (direct adapter call, surface provider pgid, SIGTERM handler)
- riskLevel: high (the atomic sentinel + provider-group kill agent; the pane process identity)
- files: phase_executor/src/phase_executor/pane_runner.py, tests/phase_executor/test_pane_runner.py
- CLI `python -m phase_executor.pane_runner <spec-file>`: read the FIXED spec (target, prompt, capture_dir,
  attempt_id, effort, permit_ref); cwd=worktree; call the resolved ADAPTER DIRECTLY (mod.run with the fixed
  attempt_id/capture_root, CF-18 — single authoritative observation.json at the KNOWN path); surface the
  provider pgid to <capture_dir>/.provider_pgid + install a SIGTERM handler killpg'ing the provider group
  (CF-17); dir-fsync the capture dir after finalize; record provider_exit_code.

### Task 3: supervisor.py — preflight + resolve_socket + launch (routing+quota AT LAUNCH) + status + await_job
- riskLevel: high (spawn path, private socket outside /tmp, launch-side quota ceiling, sentinel collection)
- files: phase_executor/src/phase_executor/supervisor.py, tests/phase_executor/test_supervisor_launch.py
- preflight (fail-closed both ways incl socket-dir creatability CF-13/CF-20); resolve_socket (/run/user/$UID
  + ~/.local/state fallback, reject /tmp + >108B, CF-2/CF-20); launch (RESOLVE routing + QuotaCoordinator.acquire
  the permit HERE CF-19; bare argv on -S socket; record pane_pid/pane_pgid/start_time + poll surfaced
  provider_pgid; running JobRecord; NEVER send-keys); status (default ambiguous exit -> exited_no_sentinel,
  quota_paused only injected CF-6); await_job (valid-obs INDEPENDENT of .incomplete, collect=>kill both groups,
  kill-fail -> completed_with_residue, timeout re-check + no-clobber CF-9/CF-12).

### Task 4: supervisor.py — two-group kill + recover + reaper
- riskLevel: high (two-group process-tree kill, quarantine-kill+retain security, resume cap, permit release)
- files: phase_executor/src/phase_executor/supervisor.py, tests/phase_executor/test_supervisor_lifecycle.py
- kill sequence (SIGTERM pane group -> pane_runner handler kills provider group; grace; SIGKILL pane group +
  killpg surfaced provider_pgid if alive; scan BOTH groups dead ignoring Z-state; then kill-session; release
  permit CF-17); recover (adopt / quarantine kill-both+_retain / relaunch<cap machine-signal identity assert +
  session_id==provider_session_id / fail CF-6/CF-7/CF-10); reap (both-group confirmed-death-first, derived
  liveness = has-session+capture mtime, W3 retention on dirty worktrees, exact-segment run scope, release
  orphaned permits CF-8/CF-11/CF-19); run_seat_tmux sync wrapper.
