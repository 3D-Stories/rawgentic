"""phase_executor — deterministic model-seat execution engine.

Public API is finalized in a later task; import submodules directly for now
(e.g. ``from phase_executor.contract import Observation``).
"""

__version__ = "0.1.0"
